1、面向对象和面向过程区别：
    （1)面向过程：分析解决问题的步骤。用函数一一实现，再一步步调用。性能较高。单片机就是面向过程
    (2)面向对象：建立对象为了描述事物在解决整个问题的过程中，所发生的行为。面向对象有封装、继承、多态特性
        面向对象特性：封装、继承、多态
        面向对象好处：可复用、易扩展、易维护
        性能上：比面向过程低


2、八种基本数据类型 和 他们的封装类
  byte -> Byte 1个字节
  short->Short  2个
  int -> Integer 4个
  long -> Long 8个
  （以上四个是整型）
  float -> Float 4个
  double -> Double 8个
  （上面两个浮点型）
  boolean -> Boolean 未知
  char -> Character 2个



3、instanceof的使用
    int i= 3;
    print(i instanceof Integer)  //这个是错误的，不能是基本数据类型




4、Integer的缓存问题：
    Integer i1 = 100;
    Integer i2 = 100;
    //按道理 i1==i2是什么样呢。
    直接申明数字-》自动装箱装成Integer,隐性调用valueOf方法。--》valueOf方法种，如果i在 【-128，127】之间，则从缓存中获取，并返回。否则再new 一个Integer



5、重载和重写的问题：
    重载：在同一个类中，参数列表不同。
    重写：在类与类继承的关系中，子类重写父类方法。参数、方法名、返回类型相同。
        重写注意的点：1、子类重写方法的访问修饰符一定要大于父类原有的方法
                    2、子类重写的方法一定不能抛出一个更大的异常。